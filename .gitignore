# ── Environment Variables (CRITICAL — never commit) ──────────
.env
.env.local
.env.*.local
.env*.local
!.env.example
avon-backend/.env
avon-dashboard/.env.local

# ── Dependency Directories ───────────────────────────────────
node_modules/
**/node_modules/

# ── Build Output ─────────────────────────────────────────────
.next/
out/
dist/
build/
*.tsbuildinfo

# ── Debug & Logs ─────────────────────────────────────────────
debug.txt
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS Files ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE & Editor ─────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Telemetry & Memory (may contain sensitive runtime data) ──
memory/
ollama_list.txt
qr_code_output.txt

# ── WhatsApp Web JS (browser session — contains secrets) ─────
.wwebjs_auth/
.wwebjs_cache/
